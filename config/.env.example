# Copy this file to config/.env (or place .env next to SPOT.exe) and add your key.
GEMINI_API_KEY=
# Optional: change the Gemini model used by auto-training.
GEMINI_MODEL=gemini-2.0-flash
# Optional: confidence required to auto-accept a Gemini suggestion.
AUTO_ACCEPT_CONFIDENCE=0.80
